# Core LLM clients (optional if using HTTP fallbacks)
openai>=1.30.0
google-generativeai>=0.6.0

# HTTP + YAML
requests>=2.31.0
PyYAML>=6.0.1

# PDF parsing (for building/updating the vault, optional at runtime)
PyPDF2>=3.0.1
