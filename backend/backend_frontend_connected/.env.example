MONGO_URI=mongodb://localhost:27017
MONGO_DB=agentic

JWT_SECRET=change_me
JWT_ALG=HS256
JWT_ACCESS_TTL_MIN=15
JWT_REFRESH_TTL_DAYS=7
PASSWORD_HASH_SCHEME=bcrypt

# Agent execution mode:
# - "subprocess" (default): runs `python agent8/main.py ...`
# - "inproc": imports agent8.turn_api.run_turn(...) (no CLI)
AGENT_RUN_MODE=subprocess

# Paths for your existing agent pipeline
AGENT_CONFIG_DIR=agent8/config
AGENT_STATE_BASE=agent8/state/chats     # per-chat state file is created here
AGENT_OUT_BASE=agent8/out/chats         # per-chat outputs are created here

# Python executable for subprocess mode
PYTHON_EXEC=python

# --- Core DB ---
MONGO_URI=mongodb://localhost:27017
MONGO_DB=agentic

# --- JWT ---
JWT_SECRET=change_me
JWT_ALG=HS256
JWT_ACCESS_TTL_MIN=15
JWT_REFRESH_TTL_DAYS=7

# --- Agent runner ---
AGENT_RUN_MODE=subprocess
AGENT_CONFIG_DIR=agent8/config
AGENT_STATE_BASE=agent8/state/chats
AGENT_OUT_BASE=agent8/out/chats
PYTHON_EXEC=python

# --- OpenAI ---
OPENAI_API_KEY=sk-xxxx

# --- Optional memory mirror ---
MEMORY_DB_ENABLED=false
MEMORY_SUMMARIZER_ENABLED=false
MEMORY_SUMMARIZER_USE_LLM=false
